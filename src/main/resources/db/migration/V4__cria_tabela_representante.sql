CREATE TABLE T_ESG_REPRESENTANTE (
    ID_REPRESENTANTE NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOME VARCHAR2(100) NOT NULL,
    EMAIL VARCHAR2(100) NOT NULL,
    TELEFONE VARCHAR2(20),
    DATA_CADASTRO TIMESTAMP
);
